// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
}

datasource dbsql {
  provider     = "mysql"
  url          = env("DATABASE_URL")
  relationMode = "prisma"
}

enum Role {
  User
  ADMIN
}

model User {
  user_id        Int     @id @default(autoincrement())
  role           Role    @default(User)
  name           String  @unique
  email          String  @unique
  password       String?
  user_bio       String?
  picture_url    String?
  login_provider String?

  institutions      Institution[] @relation(name: "institutionsByUserId")
  created_projects  Projects[]    @relation(name: "createdByUserId")
  active_projects   Projects[]    @relation(name: "activedByUserId")
  favorite_projects Projects[]    @relation(name: "favoritesByUserId")
  admin_projects    Projects[]    @relation(name: "adminProjectsByUserId")
  pending_projects  Projects[]    @relation(name: "pendingProjectsByUserId")

  is_active  Boolean  @default(true)
  is_deleted Boolean  @default(false)
  created_at DateTime @default(now())
  updated_at DateTime @default(now())
}

model Institution {
  institucion_id Int    @id @default(autoincrement())
  userUser_id    Int?
  User           User[] @relation(name: "institutionsByUserId")
}

model Projects {
  content            String?
  post_id            Int     @id @default(autoincrement())
  title              String
  author             User?   @relation(name: "createdByUserId", fields: [author_id], references: [user_id])
  active_users       User[]  @relation(name: "activedByUserId")
  favorite_users     User[]  @relation(name: "favoritesByUserId")
  admin_users        User[]  @relation(name: "adminProjectsByUserId")
  pending_users      User[]  @relation(name: "pendingProjectsByUserId")
  active_users_id    Int?
  favorite_users_ids Int?
  author_id          Int?
  admin_users_id     Int?
  pending_users_id   Int?

  @@index([author_id, favorite_users_ids, active_users_id, admin_users_id, pending_users_id])
}
